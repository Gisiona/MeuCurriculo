namespace MeuCurriculo.Constantes
{
    public sealed class CEmail
    {
        private const string MAIL_SERVER = "MailServer";
        private const string PORT = "Port";
        private const string ENABLE_SSL = "EnableSSL";
        private const string EMAIL_FROM = "EmailFromAddress";
        private const string MAIL_AUTH_USER = "MailAuthUser";
        private const string MAIL_AUTH_PASS = "MailAuthPass";        
    }
}
